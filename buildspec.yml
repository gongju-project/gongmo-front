version: 0.2

phases:
  install: 
    commands:
      - pip install --upgrade pip
      - pip install --upgrade awscli
      - mkdir ./temporary
      - aws s3 sync ./temporary s3://build.cdk/gongmo-front/
      - cd ./temporary
      - npm install
  build:
    commands:
      - cdk synth
      - cdk deploy