version: 0.2

phases:
  install: 
    commands:
      - pip install --upgrade pip
      - pip install --upgrade awscli
      - aws s3 sync . s3://build.cdk/gongmo-front/
      - cd gongmo-front
      - npm install
  build:
    commands:
      - cdk synth
      - cdk deploy